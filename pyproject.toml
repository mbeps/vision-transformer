[project]
name = "vision-transformer-model"
version = "0.1.0"
description = "Building a Vision Transformer Model from Scratch using PyTorch"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mypy>=1.19.0",
    "pydantic>=2.12.5",
]

[project.scripts]
mcp-server = "mcp_server.main:run"

[project.optional-dependencies]
default = [
    "torch>=2.2.0,<3.0",
    "torchvision>=0.17.0,<1.0",
    "numpy>=1.26.0",
    "matplotlib>=3.8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mcp_server"]

[dependency-groups]
dev = [
    "mypy>=1.19.0",
    "pytest>=8.3.3",
    "ruff>=0.14.9",
]

[tool.ruff]
line-length = 120
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I"]
